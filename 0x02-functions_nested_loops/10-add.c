#include"main.h"
#include<stdio.h>
/**
 * add - Function that adds two integers
 * @x: one of the integerss
 * @y: the other integer
 * Return: Value of the reult
 */
int add(int x, int y)
{
	int result = x + y;

	return (result);
}
